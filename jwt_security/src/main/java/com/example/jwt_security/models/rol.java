package com.example.jwt_security.models;

public enum rol {
	
	/*
	 * Que significa enum: clase abstracta  que todos los tipos de enumeracion
	 * 
	 * 
	 * en este archivo se van a crear los recursos estaticos 
	 * Admin
	 * User*/
	Admin,
	User
}
